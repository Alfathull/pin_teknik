// Sitemap XML - Updated version
Route::get('/sitemap.xml', function () {
    $urls = [
        // Homepage
        [
            'url' => url('/'),
            'lastmod' => now()->toISOString(),
            'changefreq' => 'weekly',
            'priority' => '1.0'
        ],
        
        // Product Pages (sesuai dengan routes yang ada)
        [
            'url' => url('/produk/pin-enamel-custom-premium'),
            'lastmod' => now()->toISOString(),
            'changefreq' => 'monthly',
            'priority' => '0.9'
        ],
        [
            'url' => url('/produk/medali-prestasi-custom'),
            'lastmod' => now()->toISOString(),
            'changefreq' => 'monthly',
            'priority' => '0.9'
        ],
        [
            'url' => url('/produk/label-hijab-bordir'),
            'lastmod' => now()->toISOString(),
            'changefreq' => 'monthly',
            'priority' => '0.8'
        ],
        [
            'url' => url('/produk/souvenir-gantungan-kunci'),
            'lastmod' => now()->toISOString(),
            'changefreq' => 'monthly',
            'priority' => '0.8'
        ],
        [
            'url' => url('/produk/atribut-tni-polri'),
            'lastmod' => now()->toISOString(),
            'changefreq' => 'monthly',
            'priority' => '0.8'
        ]
    ];

    $xml = '<?xml version="1.0" encoding="UTF-8"?>' . PHP_EOL;
    $xml .= '<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">' . PHP_EOL;
    
    foreach ($urls as $url) {
        $xml .= '  <url>' . PHP_EOL;
        $xml .= '    <loc>' . $url['url'] . '</loc>' . PHP_EOL;
        $xml .= '    <lastmod>' . $url['lastmod'] . '</lastmod>' . PHP_EOL;
        $xml .= '    <changefreq>' . $url['changefreq'] . '</changefreq>' . PHP_EOL;
        $xml .= '    <priority>' . $url['priority'] . '</priority>' . PHP_EOL;
        $xml .= '  </url>' . PHP_EOL;
    }
    
    $xml .= '</urlset>';

    return response($xml)->header('Content-Type', 'application/xml');
})->name('sitemap');